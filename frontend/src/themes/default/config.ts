import type { ThemeConfig } from '../types';

export const defaultTheme: ThemeConfig = {
  id: 'default',
  label: 'Default',
  description: 'Neutral lab interface for core workflows.',
  accent: '#101418',
  surfaceClassName: 'bg-white'
};
