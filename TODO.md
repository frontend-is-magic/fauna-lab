# Fauna Lab TODO

最后更新：2026-04-13

## 当前进度

- [x] 创建 GitHub 仓库并启用 `main` 分支保护
- [x] 落地项目协作规约与技术决策到 `AGENTS.md`
- [x] 初始化前端项目骨架
- [x] 初始化后端项目骨架
- [x] 将前端路由改为约定式路由
- [x] 本地完成前端生产构建验证
- [x] 本地启动后端并通过 `/health` 检查
- [ ] 整理并提交本地跑通过程中产生的必要修复

## 前端 TODO

- [x] 约定式路由主链打通：`main.tsx -> App.tsx -> src/router.tsx -> src/pages/*`
- [x] 保留 `default / zoo` 主题切换入口
- [x] 补齐 `vite-env.d.ts`，修复 `import.meta.glob` / `import.meta.env` 类型问题
- [x] 补齐 `Dataset` 页面真实业务 UI，包括文件夹上传、预览和外置存储设置
- [x] 为数据集与存储设置建立前端 API 类型层
- [ ] 为 `frontend/src/router.tsx` 增加更稳定的路由约束与测试覆盖
- [ ] 补齐 `Train`、`Inference`、`Models` 三个页面的真实业务 UI
- [ ] 跑通 `pnpm lint`
- [ ] 跑通 `pnpm typecheck`

## 后端 TODO

- [x] FastAPI 应用入口、路由和服务占位已就位
- [x] `/health` 本地可用
- [ ] 实现数据集上传、预览、删除的真实文件操作
- [ ] 实现训练任务启动、停止、状态轮询与历史记录
- [ ] 实现推理接口与模型加载
- [ ] 实现模型列表、下载、删除
- [ ] 增加后端最小冒烟测试

## 集成 TODO

- [ ] 对齐前端 `fetch` 请求层与后端实际响应结构
- [x] 验证 `Dataset` 页面到后端接口的最小联调链路
- [ ] 验证其余前端页面到后端接口的最小联调链路
- [ ] 增加 README 的本地启动说明
- [ ] 增加 PR 检查命令说明

## 风险与备注

- 当前前端能正常构建，但本地运行后尚未做页面级手工验收。
- 当前后端已可启动，但业务接口多数仍为占位实现。
- 前端构建会生成本地产物与配置输出文件，这些应忽略，不应提交入库。
